{
  "schema": 2,
  "fingerprintAlgorithm": "sha256",
  "canonicalCatalogFormat": "myserver-postgresql-catalog-v1",
  "databases": {
    "auth": { "fingerprints": [] },
    "game": { "fingerprints": [] },
    "chat": { "fingerprints": [] },
    "announce": { "fingerprints": [] },
    "mail": { "fingerprints": [] }
  },
  "reviewedFingerprintEntry": {
    "required": ["sha256", "version", "description"],
    "versionFormat": "YYYYMMDDHHMMSS",
    "descriptionFormat": "SQLx migration description derived from the approved filename"
  },
  "note": "No catalog fingerprint is approved until it is captured from a reviewed PostgreSQL baseline. Every approved fingerprint must bind the exact reviewed migration version and description; unknown fingerprints or target versions must be rejected."
}
