# 主世界跨端联调与最终验收 Checklist

## 目标

在客户端静态世界、方圆多人实体、摄像机操控、客户端移动和 MyServer 4000 米 authority 均完成后，执行主世界跨端集成、双客户端联调、断线恢复、生命周期、视觉、性能和平台验收，确认完整产品结果可以交付。

本清单以发现和修复集成缺口为目标，不在没有证据时重做前置模块。若联调暴露独立架构缺陷，应回到对应前置 checklist 补充实现和验证，再返回本清单复验。

## 前置条件

- `世界联机_01_主世界4000米静态场景与照明_checklist.md` 已完成。
- `世界联机_02_方圆玩家共享渲染与主世界多人实体_checklist.md` 已完成。
- `世界联机_03_主世界摄像机与桌面触控视角操控_checklist.md` 已完成。
- `世界联机_04_主世界客户端移动预测纠正与远端插值_checklist.md` 已完成。
- `世界联机_05_MyServer主世界4000米权威移动_checklist.md` 已完成。
- 现有主世界登录、选角、加房、Scene Ready、Room Ready、退出和家园往返基线保持可用。
- 登录与真实联调环境约束：凡需要账号登录、选角、加房或实际移动验证，优先尝试正式服登录；本地服务端当前用于其他功能测试，不得默认启动、重启或占用。只有正式服不可用且用户明确授权时，才改用本地服务端。

## 基础原则

- [ ] 联调使用 character-bound ticket 和不同 `character_id`，不以账号 `player_id` 替代玩法主体。
- [ ] 客户端与服务端版本必须同时满足同一 4000 米坐标和 movement policy 契约。
- [ ] 自动测试、日志、截图和性能数据只记录可复现证据，不凭观察臆造通过结果。
- [ ] Android 真机项需要满足现有公网部署与真机验收清单的前置条件，不用桌面结果替代。
- [ ] 每个阶段完成后运行对应验证，并按阶段独立提交。

## 阶段 1：跨端版本与契约预检

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 记录参与联调的 mybevy 与 MyServer 精确 commit、配置版本和协议生成来源。
- [ ] 核对 `world.main` 与 `grassland_01 / scene_id=1 / spawn_id=1001` 映射未变化。
- [ ] 核对公共房间为 `main-world-public / movement_demo`，两端 tick 为 20Hz、速度为 4m/s。
- [ ] 核对服务端坐标 `0..4000`、客户端中心偏移 2000 米和默认出生点 `(2002,2002) -> (2,0,2)`。
- [ ] 核对 MoveInput、EntityTransform、MovementSnapshot、MovementReject 和 recovery Protobuf schema 一致。
- [ ] 确认服务端 movement AOI 关闭，客户端 full sync 按完整房间实体集合处理。
- [ ] 检查两个仓库工作区状态，保留并记录与本轮无关的既有改动，不混入联调修复提交。

## 阶段 2：正式服单客户端基线

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 优先使用正式服登录链路，不启动或重启本地 Redis、数据库、auth、proxy、game-server；如正式服不可用，记录阻塞原因并等待用户明确授权切换。
- [ ] 使用正式服有效账号、角色和 character-bound ticket 完成登录、选角和 game proxy 鉴权。
- [ ] 加入 `main-world-public`，验证 RoomJoin、初始 movement snapshot、Scene Ready、Room Ready 和 Active 顺序。
- [ ] 确认默认出生坐标映射为客户端 `(2,0,2)`，本地方圆玩家只生成一次且脚底贴地。
- [ ] 确认 4000 米地面、单 Mesh 参照球和日间照明正常显示。
- [ ] 确认摄像机默认跟随本地玩家，环绕、俯仰和缩放参数不会被下一帧覆盖。
- [ ] 保存正式服单客户端日志、关键状态和固定窗口截图作为后续对照基线，并记录环境为 production。

## 阶段 3：双客户端实体与移动同步

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 使用两个不同 `character_id` 的客户端通过正式服加入同一公共房间；不得默认通过本地服务端完成此项。
- [ ] 两端分别确认恰好生成一个本地玩家和一个对应远端玩家，无重复或身份交换。
- [ ] 客户端 A 移动和停止时，客户端 B 看到对应远端角色连续位移并收敛停止。
- [ ] 客户端 B 移动和停止时，客户端 A 得到对称结果。
- [ ] 验证本地角色采用预测与纠正，远端角色采用插值，没有互换显示策略。
- [ ] 验证两端摄像机只跟随各自本地 character，远端玩家不会抢占 target。
- [ ] 验证 full sync、普通增量、重复和旧帧不会重复生成实体或回退位置。
- [ ] 记录双客户端网络帧、快照频率、可见实体数和关键延迟数据。

## 阶段 4：移动、视角和边界验收

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 验证 WASD、方向键和触控摇杆均按当前摄像机平面方向移动。
- [ ] 验证斜向速度与单轴速度一致，服务端权威速度保持 4m/s。
- [ ] 验证移动期间按 20Hz 提交输入，释放、失焦和 UI 阻断只产生必要停止语义。
- [ ] 验证桌面右键拖动、滚轮、触控右侧拖动和双指缩放不干扰移动输入。
- [ ] 使用测试或调试定位验证服务端 0、中心、接近 4000 和越界坐标，不人工步行穿越全图。
- [ ] 确认合法边界位置能同步，负值、4000 排他上界、NaN 和 Infinity 被拒绝。
- [ ] 验证 100 米参照球可以辅助判断方向和位移，不要求摄像机一次显示完整地图。

## 阶段 5：纠正、拒绝与异常网络

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 构造小幅客户端预测误差，确认使用平滑纠正且不持续振荡。
- [ ] 构造超过阈值的漂移，确认服务端下发权威 correction 并使客户端收敛。
- [ ] 构造非法方向或越界移动，确认 `MovementRejectPush` 的 corrected transform 被正确应用。
- [ ] 验证乱序、重复、丢失和迟到快照不会回退已应用权威状态。
- [ ] 验证移动控制包短暂缺失后服务端按 control timeout 停止角色。
- [ ] 检查客户端和服务端日志包含必要 room、character、frame 和 reason，同时不包含 ticket、密码或 token。
- [ ] 记录正常网络和受控异常网络下的预测误差、纠正频率和远端插值表现。

## 阶段 6：断线恢复与场景生命周期

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 移动中断开 game proxy，确认客户端立即冻结输入且不无限延伸本地预测。
- [ ] 完成重鉴权和 RoomReconnect，确认 recovery snapshot 恢复位置、方向、moving 和玩家集合。
- [ ] 确认恢复后本地摄像机跟随有效新实体，未绑定旧 scene Entity。
- [ ] 验证 server redirect 期间旧连接迟到快照不覆盖新 generation 权威状态。
- [ ] 返回 Lobby 后确认停止发送移动输入，并清理玩家、参照球、摄像机目标和运行时缓存。
- [ ] 进入家园再返回主世界，确认重新加入公共房间且不残留旧实体或旧预测历史。
- [ ] 重复进入/退出主世界，确认 SceneOwned Entity、3D Camera、环境光和输入 reader 不累积。
- [ ] 验证 session kick 和不可恢复鉴权失败沿现有路径清理并返回登录页。

## 阶段 7：性能、视觉与平台验收

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 从仓库根运行 `scripts/run_fast.ps1`，按固定设备尺寸和缩放完成桌面视觉验收。
- [ ] 记录客户端单人和双人时的稳定帧率、内存、ECS Entity、Mesh/Material asset 和主要系统耗时。
- [ ] 确认 1681 个参照球仍为一个 Entity，静态 Mesh 不在运行时重复重建。
- [ ] 确认默认画面亮度、玩家轮廓、地面和参照球在出生点及移动过程中清晰可辨。
- [ ] 记录 MyServer 最大 32 人全房间快照的序列化大小、发送频率和处理开销证据。
- [ ] 如目标版本已完成公网部署，按现有 Android 真机验收清单使用正式服验证触控、前后台、断线和恢复；未满足前置条件时明确记录为外部验收项，不伪造通过。
- [ ] 在 `project/` 运行 `cargo fmt`、`cargo check` 和相关客户端测试；按 MyServer 规则经确认后运行服务端定向测试。

## 阶段 8：文档同步与清单归档准备

- 开始时间：
- 结束时间：
- 开发总结：
- 验证记录：

- [ ] 汇总两端最终坐标、速度、tick、纠正、AOI、出生点和协议兼容契约。
- [ ] 更新 `docs/scene/` 中主世界场景、摄像机、玩家生命周期和移动同步说明。
- [ ] 更新 `docs/myserver/` 中客户端接线、移动恢复和服务端配置说明。
- [ ] 检查 `docs/ui/` 是否需要补充移动摇杆、触控区域和 gameplay 输入阻断边界。
- [ ] 检查 `CLAUDE.md` 与 `docs/bevy-getting-started.md` 是否因命令、目录、初始化或上手流程变化需要同步。
- [ ] 将所有日志、截图和性能数据路径写入对应阶段验证记录，不提交临时敏感数据或无关构建产物。
- [ ] 所有拆分 checklist 完成后，按仓库约定将其从 `summary/` 归档到对应 `docs/<领域>/checklists/`。

## 最终完成定义

以下项目作为整体完成标准，不要求每个开发阶段都执行，由所有相关阶段完成后统一验收。

- 开始时间：
- 结束时间：
- 验收总结：

- [ ] 主世界呈现清晰可读的日间 `4000m x 4000m` 地面和单实体参照球 Mesh。
- [ ] 当前登录 character 生成尺寸正确、脚底贴地的方圆玩家。
- [ ] 桌面和触控均可自由移动角色并环绕、俯仰、缩放查看场景。
- [ ] 摄像机参数通过 `SceneCameraRig` 持续生效且只跟随本地玩家。
- [ ] MyServer 对 4000 米范围保持移动 authority，客户端预测和纠正行为稳定。
- [ ] 两个客户端能够看到彼此角色连续移动、停止和权威纠正。
- [ ] Reject、断线恢复、redirect、退出、家园切换和旧 generation 隔离全部通过。
- [ ] 性能、日志、安全、生命周期和文档验收均有可复现记录。
- [ ] 本阶段未引入地图分块、LOD、浮动原点、复杂地形或重复移动协议。
