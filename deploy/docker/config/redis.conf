appendonly yes
appendfsync everysec
# Redis only stores recoverable runtime state. Disable Redis's default RDB save
# rules so forked snapshots cannot contend with AOF fsync and expire ID leases.
save ""
maxmemory 512mb
maxmemory-policy noeviction
protected-mode yes
