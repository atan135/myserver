# 服务发现迁移基线

本文是统一服务发现迁移的 M0 基线，只盘点当前代码中的注册事实。后续还需要继续补充：消费发现状态、固定配置扫描、统一入口/准入/本地 fallback 口径。

端口和地址只记录本地默认监听值或默认发布值，用于理解现状；不要把这些默认值当作测试或线上推荐直连配置。

## 当前注册状态

当前 Redis service registry 的通用数据模型来自 `packages/service-registry`：

- Rust 服务使用 `packages/service-registry/src/client.rs` 的 `RegistryClient`，实例写入 `service:<service>:instances:<instance>` 的 `data` 字段，心跳写入 `heartbeat:<service>:<instance>`，默认心跳 TTL 为 30 秒。
- Node 服务各自有本地 `src/registry-client.js`，但复用 `packages/service-registry/node/registry-schema.js` 生成 payload 和 key。它们同样写 `data` 字段和 `heartbeat:<service>:<instance>`。
- Node 周边服务配置里有 `REGISTRY_ENABLED`/`DISCOVERY_REQUIRED` 用于发现侧校验，但自身注册启动代码当前直接调用 `registryClient.register()`；注册失败只记录 `startup.registry_failed`，不会因为默认 `REGISTRY_ENABLED=false` 自动跳过注册。

| 服务 | 当前是否注册 | 注册实现位置 | service name / instance id 来源 | 复用方式 |
|---|---|---|---|---|
| `game-server` | `REGISTRY_ENABLED=true` 时注册 | `apps/game-server/src/main.rs`，配置在 `apps/game-server/src/config.rs` | `SERVICE_NAME` 默认 `game-server`；`SERVICE_INSTANCE_ID` 默认 `<service_name>-<GAME_PORT>`，本地默认 `game-server-7000` | Rust `packages/service-registry` |
| `game-proxy` | `REGISTRY_ENABLED=true` 时注册 | `apps/game-proxy/src/main.rs`，配置在 `apps/game-proxy/src/config.rs` | `SERVICE_NAME` 默认 `game-proxy`；`SERVICE_INSTANCE_ID` 默认 `<service_name>-<PROXY_PORT>`，本地默认 `game-proxy-4000` | Rust `packages/service-registry` |
| `auth-http` | 启动时尝试注册 | `apps/auth-http/src/main.ts`、`apps/auth-http/src/registry-client.js`、注入在 `apps/auth-http/src/app.module.ts` | `SERVICE_NAME` 默认 `auth-http`；`SERVICE_INSTANCE_ID` 默认 `auth-http-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js` |
| `chat-server` | `REGISTRY_ENABLED=true` 时注册 | `apps/chat-server/src/main.rs` | `SERVICE_NAME` 默认 `chat-server`；`SERVICE_INSTANCE_ID` 默认 `chat-server-001` | Rust `packages/service-registry` |
| `match-service` | `REGISTRY_ENABLED=true` 时注册 | `apps/match-service/src/main.rs`，配置在 `apps/match-service/src/config.rs` | `SERVICE_NAME` 默认 `match-service`；`SERVICE_INSTANCE_ID` 默认 `match-service-<MATCH_BIND_ADDR port>`，本地默认 `match-service-9002` | Rust `packages/service-registry` |
| `mail-service` | 启动时尝试注册 | `apps/mail-service/src/main.ts`、`apps/mail-service/src/registry-client.js` | `SERVICE_NAME` 默认 `mail-service`；`SERVICE_INSTANCE_ID` 默认 `mail-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js` |
| `announce-service` | 启动时尝试注册 | `apps/announce-service/src/main.ts`、`apps/announce-service/src/registry-client.js` | `SERVICE_NAME` 默认 `announce-service`；`SERVICE_INSTANCE_ID` 默认 `announce-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js` |
| `admin-api` | 启动时尝试注册 | `apps/admin-api/src/main.ts`、`apps/admin-api/src/registry-client.js` | `SERVICE_NAME` 默认 `admin-api`；`SERVICE_INSTANCE_ID` 默认 `admin-api-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js`，并复用 discovery helper 刷新 game-server/game-proxy admin endpoint |
| `metrics-collector` | 不注册 | `apps/metrics-collector/src/server.js`、`apps/metrics-collector/src/registry-client.js`、配置在 `apps/metrics-collector/src/config.js` | `SERVICE_NAME` 默认 `metrics-collector`；`SERVICE_INSTANCE_ID` 默认 `metrics-collector-001`，仅用于状态输出 | 显式禁止注册；没有真实可连接 endpoint |

### 发布 endpoints

| 服务 | 当前发布 endpoints | 本地默认监听/发布值参考 |
|---|---|---|
| `game-server` | `client` / `tcp` / `internal`；`admin` / `tcp` / `admin`；`internal` / `local_socket` / `local`；`proxy-local` / `local_socket` / `local` | `GAME_PORT=7000`；`ADMIN_PORT=7500`；`GAME_INTERNAL_SOCKET_NAME` 默认 `myserver-game-server-internal.sock`；`GAME_LOCAL_SOCKET_NAME` 默认 `myserver-game-server.sock` |
| `game-proxy` | `client` / `kcp` / `public`；`client-tcp-fallback` / `tcp` / `public`；`admin` / `http` / `admin` | `PROXY_PORT=4000`；`PROXY_TCP_FALLBACK_PORT` 默认 `PROXY_PORT + 10000`，即 `14000`；`PROXY_ADMIN_PORT=7101` |
| `auth-http` | `http` / `http` / `public`；`internal` / `http` / `internal` | `PORT=3000` |
| `chat-server` | `tcp` / `tcp` / `internal` | `CHAT_BIND_ADDR=0.0.0.0:9001`，发布 host 会规避 `0.0.0.0`/`::` 并回退到 `127.0.0.1` |
| `match-service` | `grpc` / `grpc` / `internal` | `MATCH_BIND_ADDR=0.0.0.0:9002`，发布 host 会规避 `0.0.0.0`/`::` 并回退到 `127.0.0.1` |
| `mail-service` | `http` / `http` / `internal` | `MAIL_PORT=9003` |
| `announce-service` | `http` / `http` / `internal` | `ANNOUNCE_PORT=9004` |
| `admin-api` | `http` / `http` / `admin` | `PORT=3001` |
| `metrics-collector` | 无 | 无监听 endpoint；消费 NATS metrics 并写 Redis snapshots |

各服务发布 host 都有“不要发布通配监听地址”的处理：Rust 服务在 `build_service_instance` 附近通过 `published_host`/`advertised_host_from_env` 归一化，Node 服务在各自 `registry-client.js` 的 `publishedHostFromConfig` 中归一化。

### Heartbeat 与 deregister

| 服务 | heartbeat 行为 | deregister 行为 |
|---|---|---|
| `game-server` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，间隔来自 `REGISTRY_HEARTBEAT_INTERVAL`，默认 10 秒，TTL 30 秒 | `server::run` 返回后 abort heartbeat task，再 `deregister()` 删除实例和 heartbeat key |
| `game-proxy` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，当前未调用 `with_heartbeat_interval`，使用 registry 包默认 10 秒，TTL 30 秒 | `proxy_server::run` 返回后 abort heartbeat task，再 `deregister()` 删除实例和 heartbeat key |
| `chat-server` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，间隔来自 `REGISTRY_HEARTBEAT_INTERVAL`，默认 10 秒，TTL 30 秒 | 主循环返回后释放相关任务，再 `deregister()` 删除实例和 heartbeat key |
| `match-service` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，间隔来自 `REGISTRY_HEARTBEAT_INTERVAL`，默认 10 秒，TTL 30 秒 | `server::run` 返回后 abort heartbeat task，再 `deregister()` 删除实例和 heartbeat key |
| `auth-http` | `main.ts` 调用 `startHeartbeat(10)`；Node client 立即 `setex` 一次，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()`，再 `deregister()` 删除实例和 heartbeat key |
| `mail-service` | `main.ts` 调用 `startHeartbeat(10)`；立即写 heartbeat，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()`，再 `deregister()` 删除实例和 heartbeat key |
| `announce-service` | `main.ts` 调用 `startHeartbeat(10)`；立即写 heartbeat，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()`，再 `deregister()` 删除实例和 heartbeat key |
| `admin-api` | `main.ts` 调用 `startHeartbeat(10)`；立即写 heartbeat，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()` 和 `stopDiscoveryRefresh()`，再 `deregister()` 删除实例和 heartbeat key |
| `metrics-collector` | 无 registry heartbeat；业务层只写 `metrics:heartbeat:<service>` 和 `metrics:heartbeat:<service>:<instance>` 表示 metrics 数据新鲜度 | 无 registry deregister |

### metrics-collector 不注册的代码依据

`apps/metrics-collector/src/registry-client.js` 的 `maybeRegisterService()` 默认返回 `registered: false`，原因文本是：它消费 NATS metrics 并写 Redis snapshots，但没有可连接服务 endpoint。若设置 `SERVICE_REGISTRY_REGISTER=true`，该函数会直接抛错，说明在暴露真实 `http`/`tcp`/`local_socket` endpoint 之前不支持注册。

因此 `metrics-collector` 当前不是服务发现生产者。它写入的 `metrics:*` 和 `metrics:heartbeat:*` key 是指标快照与指标新鲜度，不是 service registry 实例记录。

## 当前消费发现状态

本节只盘点当前代码中已经消费 Redis service registry 的路径。端口只作为本地默认或手工 fallback 示例；测试、预发和线上不应把这些默认端口当作推荐直连配置。

| 消费者 / 链路 | 发现目标 | endpoint | 代码路径 | fallback | fail-fast / 严格模式 | 缓存或刷新 |
|---|---|---|---|---|---|---|
| `auth-http` 登录返回 `services.game` 和兼容字段 | `game-proxy` | `client`，优先按名称发现，返回给客户端时强制协议字段为 `kcp` | `apps/auth-http/src/service-discovery.js`、`apps/auth-http/src/auth/auth.service.ts`、`apps/auth-http/src/config.js` | 仅本地 fallback 模式可用 `GAME_PROXY_HOST` / `GAME_PROXY_PORT`，并继续填旧兼容字段 `gameProxyHost` / `gameProxyPort`；非本地模式会忽略这些直连配置并用默认占位值 | `DISCOVERY_REQUIRED=true` 或 `NODE_ENV` / `APP_ENV` 为 `production` / `test` 时要求 `REGISTRY_ENABLED=true`；注册表禁用或 `game-proxy.client` 缺失会抛 `SERVICE_DISCOVERY_UNAVAILABLE`，登录失败 | `RegistryDiscoveryClient` 使用 `registryDiscoveryCacheTtlMs`；登录/注册/游客登录每次构造成功响应时调用发现 |
| `auth-http` 登录返回可选周边 endpoint | `chat-server`、`mail-service`、`announce-service` | `chat-server.tcp`、`mail-service.http`、`announce-service.http`，缺失时兼容查 `client` endpoint | `apps/auth-http/src/service-discovery.js`、`apps/auth-http/src/config.js` | 不会为 chat/mail/announce 编造本地 fallback；`REGISTRY_ENABLED=false` 时即使开启暴露开关也返回 `null` | 只有 `AUTH_EXPOSE_INTERNAL_SERVICE_ENDPOINTS=true` 且非生产环境时才暴露这些内部 endpoint；生产环境强制不暴露。严格发现主要约束 `game-proxy.client`，side endpoint 缺失当前不单独 fail-fast | 同上，复用 `RegistryDiscoveryClient` 缓存 |
| `game-server` 匹配客户端 | `match-service` | `grpc`，拼为 `http://host:port` 给 tonic gRPC client | `apps/game-server/src/match_client.rs`、`apps/game-server/src/config.rs` | 非严格且允许本地 fallback 时使用 `MATCH_SERVICE_ADDR`，默认只作为本地示例；严格模式下旧直连配置会被告警忽略，也可用 `DISALLOW_LEGACY_DIRECT_CONFIG=true` 禁止 | `DISCOVERY_REQUIRED=true` 或 `NODE_ENV` / `APP_ENV` 为 `production` / `test` 时要求 registry；注册表禁用、发现错误或 endpoint 缺失都会 panic / fail-fast。运行中调用失败会把 client 标为 `reconnect_required` | 启动解析一次；`spawn_match_client_rediscovery()` 在 registry enabled 时每 `MATCH_SERVICE_REDISCOVERY_INTERVAL_SECS` 重新发现，地址变化或连接标记需重连时重建 client；发现/重连失败保留现有 client 到下个 tick |
| `game-proxy` upstream 路由 | `game-server` | 只消费 `proxy-local` / `local_socket` / `local`，不回退到 `internal` 或 legacy client endpoint | `apps/game-proxy/src/proxy_server.rs`、`apps/game-proxy/src/route_store.rs`、`apps/game-proxy/src/config.rs`、`apps/game-proxy/src/upstream.rs` | `REGISTRY_ENABLED=false` 且非严格时，用 `UPSTREAM_SERVER_ID` + `UPSTREAM_LOCAL_SOCKET_NAME` 填静态 route store；这是本地兼容 fallback | 严格模式要求 `REGISTRY_ENABLED=true`；启动时 registry client 创建失败、初始发现失败或 `game-server.proxy-local` 数量为 0 会 fail-fast。后续 watch 发现错误或空列表不会退出进程，但会把已知路由同步为 unavailable，客户端无法选到可用 upstream | 启动先 `refresh_discovery_snapshot()` 初始化 route store；随后 `watch_discovery()` 按 `REGISTRY_DISCOVER_INTERVAL_SECS` 刷新。route store 可为 memory 或 redis，保存 upstream / room / player route 状态 |
| `match-service` 创建房间时调用游戏服内部口 | `game-server` | `internal` / `local_socket`，要求 visibility 为 `local` 或 `internal`；可按 mode、zone 和权重候选选择 | `apps/match-service/src/game_server_client.rs`、`apps/match-service/src/config.rs` | 非严格且本地 fallback 可用时，使用 `GAME_SERVER_INTERNAL_SOCKET_NAME` 或旧别名 `GAME_INTERNAL_SOCKET_NAME`；否则使用由 `GAME_LOCAL_SOCKET_NAME` 推导出的本地默认 socket 名作为示例 | `DISCOVERY_REQUIRED=true` 或 `NODE_ENV` / `APP_ENV` 为 `production` / `test` 时要求 registry；registry disabled、registry client 不可用、无 internal endpoint 或无合格候选都会返回 `RoomCreateFailed`，不会回退 | `GameServerDiscovery` 懒创建 registry client；候选列表按 `GAME_SERVER_DISCOVERY_CACHE_TTL_SECS` 缓存，默认 5 秒；每次创建匹配房间解析一次 socket |
| `admin-api` GM / 玩家控制面 | `game-server` | `admin` / `tcp` / `admin` | `apps/admin-api/src/game-admin-client.js`、`apps/admin-api/src/registry-client.js`、`apps/admin-api/src/gm/gm.controller.ts`、`apps/admin-api/src/config.js` | 仅本地 fallback 模式可用 `GAME_SERVER_ADMIN_HOST` / `GAME_SERVER_ADMIN_PORT`；非本地或严格模式禁用直连 fallback | 严格模式要求 registry enabled；发现禁用且不可 fallback 时抛 `SERVICE_DISCOVERY_REQUIRED`。多实例下 `sendItem` / `kickPlayer` / `banPlayer` / `updateConfig` 要求显式 `targetInstanceId`，缺失抛 `GAME_SERVER_ADMIN_TARGET_REQUIRED`，目标不存在抛 `GAME_SERVER_ADMIN_TARGET_NOT_FOUND`；broadcast 默认遍历所有发现 endpoint | `admin-api` 启动时 `RegistryClient.startDiscoveryRefresh()` 刷新 `game-server.admin`；发现函数优先读 refresh snapshot，未命中才扫描 registry；刷新间隔 `REGISTRY_DISCOVERY_REFRESH_INTERVAL_MS`，cache TTL `REGISTRY_DISCOVERY_CACHE_TTL_MS` |
| `admin-api` 监控 / rollout 控制面 | `game-proxy` | `admin` / `http` / `admin` | `apps/admin-api/src/monitoring/monitoring.service.ts`、`apps/admin-api/src/registry-client.js`、`apps/admin-api/src/config.js` | 仅本地 fallback 模式可用 `GAME_PROXY_ADMIN_HOST` / `GAME_PROXY_ADMIN_PORT`；`rolloutDrain()` 在错误响应里只回显本地 fallback upstream 信息 | 严格模式要求 registry enabled；`getGameProxyAdminEndpoints()` 在发现禁用且不可 fallback 时抛 `SERVICE_DISCOVERY_REQUIRED`。发现多个 game-proxy admin endpoint 时逐个请求 `/rollout` 并聚合，不要求显式 target | 同 `admin-api` refresh cache，启动时刷新 `game-proxy.admin` 的 all_endpoints |
| `mail-service` 发放邮件附件 | `game-server` | `admin` / `tcp` / `admin` | `apps/mail-service/src/game-admin-client.js`、`apps/mail-service/src/registry-client.js`、`apps/mail-service/src/config.js` | 仅本地 fallback 模式可用 `GAME_SERVER_ADMIN_HOST` / `GAME_SERVER_ADMIN_PORT`；非本地或严格模式禁用直连 fallback | 严格模式要求 registry enabled；发现禁用且不可 fallback 时抛 `SERVICE_DISCOVERY_REQUIRED`。`grantMailAttachments()` 强制 `requireExplicitTarget=true`，多实例时必须传 `targetInstanceId`，避免把附件发到不确定实例 | 当前没有后台 refresh；每次调用通过 `discoverGameServerAdminEndpoints()` 扫 registry，并按 `targetInstanceId` 解析 |
| `rollout-three-process-drill` 脚本 | `auth-http`、`game-proxy`、两个 `game-server` 实例 | `auth-http.internal` / `http`、`game-proxy.admin` / `http`、`game-server.admin` / `tcp` | `scripts/rollout-three-process-drill.ps1` | 只有 `RegistryEnabled=false` 或 `DiscoveryRequired=false` 的本地演练允许 local fallback：`AuthBaseUrl`、`ProxyAdminUrl`、old/new admin host/port | `EnvironmentName` / `APP_ENV` / `NODE_ENV` 为 prod/production/staging/test/testing 时强制 discovery required；`RegistryEnabled=true` 且 `DiscoveryRequired=true` 下 registry 失败、auth internal 缺失、proxy admin 多实例未指定、old/new game-server 实例或 admin endpoint 缺失都会 fail-fast；preflight 还会拒绝严格环境中的 local fallback | 嵌入 Node 脚本一次性从 Redis 或 fixture 读取 registry snapshot；不做长轮询刷新 |
| `chat-server` | 当前不消费 registry | 无 | `apps/chat-server/src/main.rs`、`apps/chat-server/src/mail_subscriber.rs`、`apps/chat-server/src/metrics.rs` | 无发现 fallback；它自身可注册 `tcp` endpoint | 有 `DISCOVERY_REQUIRED`/严格环境配置校验，但当前只影响自身 registry 注册是否必须成功，不发现其他服务 | 主要通过 TCP 客户端连接、NATS 邮件通知订阅和 NATS metrics 上报工作 |
| `announce-service` | 当前不消费 registry | 无 | `apps/announce-service/src/app.module.ts`、`apps/announce-service/src/config.js`、`apps/announce-service/src/metrics.js` | 无发现 fallback；它自身可注册 `http` endpoint | 有 `DISCOVERY_REQUIRED`/严格环境配置校验，但当前只影响自身 registry 配置有效性，不发现其他服务 | 主要通过 HTTP API、PostgreSQL/Redis/NATS metrics 工作 |
| `metrics-collector` | 当前不消费 service registry，也不注册 | 无 | `apps/metrics-collector/src/server.js`、`apps/metrics-collector/src/registry-client.js`、`apps/metrics-collector/src/config.js` | 无 | 设置 `SERVICE_REGISTRY_REGISTER=true` 会直接报错；没有发现失败口径 | 订阅 `NATS` 的 `myserver.metrics.>`，写 Redis `metrics:*` 和 `metrics:heartbeat:*` 快照，不读取 service registry 实例 |

补充口径：

- “严格模式”在当前实现中主要由 `DISCOVERY_REQUIRED=true` 或 `NODE_ENV` / `APP_ENV` 为 `production` / `test` 触发；`rollout-three-process-drill.ps1` 还把 `staging` / `testing` 视作严格环境。
- Node 服务的旧直连变量在非本地或严格发现下会被忽略并输出 warning；设置 `DISALLOW_LEGACY_DIRECT_CONFIG=true` 时，相关变量存在会直接拒绝启动。
- 多实例 admin 控制链路的当前边界是：读/聚合类操作可以遍历所有 endpoint，写入到单个 game-server 的操作需要显式 `targetInstanceId`。

## 固定 host/port 配置扫描

本节按当前代码仓库盘点 `.env.example`、可见 `.env`、`scripts/`、`tools/` 和 `docs/` 中的固定 host / port / addr / url / socket 配置。检索排除了 `docs/历史归档/初始设计稿/` 和 `node_modules`；结果用于迁移基线，不表示测试、预发或线上可以按固定地址直连。

### 1. 自身监听 / bind 默认值

| 文件 | 变量 / 配置 | 默认值或示例值 | 当前口径 / 风险 |
|---|---|---|---|
| `apps/auth-http/.env.example`、`apps/auth-http/.env` | `HOST` / `PORT`、`SERVICE_BIND_HOST`、`SERVICE_PUBLIC_HOST` | `127.0.0.1:3000` | `auth-http` 自身 HTTP 监听与发布 host 示例；测试/线上消费者应发现 `auth-http.internal` 或使用部署入口，不应把本地端口当直连配置。 |
| `apps/admin-api/.env.example`、`apps/admin-api/.env` | `HOST` / `PORT`、`SERVICE_BIND_HOST`、`SERVICE_PUBLIC_HOST` | `127.0.0.1:3001` | `admin-api` 自身后台 API 默认监听；属于控制面入口，不是服务间发现 fallback。 |
| `apps/game-server/.env.example`、`apps/game-server/.env` | `GAME_HOST` / `GAME_PORT`、`SERVICE_BIND_HOST`、`SERVICE_PUBLIC_HOST` | `127.0.0.1:7000` | `game-server` 玩家 TCP 本地默认；正式客户端入口应经 `game-proxy`，不应把 `game-server:7000` 作为测试/线上入口。 |
| `apps/game-server/.env.example`、`apps/game-server/.env` | `ADMIN_HOST` / `ADMIN_PORT`、`SERVICE_ADMIN_BIND_HOST`、`SERVICE_ADMIN_ADVERTISED_HOST` | `127.0.0.1:7500` | `game-server.admin` 发布 endpoint 的本地默认；消费者严格模式通过 registry 发现，不按 host/port 静态直连。 |
| `apps/game-server/.env.example` | `GAME_LOCAL_SOCKET_NAME`、`GAME_INTERNAL_SOCKET_NAME` | `myserver-game-server.sock`、`myserver-game-server-internal.sock` | 本机 local socket 默认名；`GAME_INTERNAL_SOCKET_NAME` 仍作为同机本地 fallback 示例，测试/线上应使用 registry 发布的 `internal` / `proxy-local` endpoint。 |
| `apps/game-proxy/.env.example`、`apps/game-proxy/.env` | `PROXY_HOST` / `PROXY_PORT` | `.env.example` 为 `127.0.0.1:4000`；可见 `.env` 为 `127.0.0.1:7002` | `game-proxy.client` KCP 本地默认或本机覆盖；客户端应从登录响应 `services.game` 获取实际 endpoint。 |
| `apps/game-proxy/.env` | `PROXY_TCP_FALLBACK_HOST` / `PROXY_TCP_FALLBACK_PORT` | `127.0.0.1:17002` | 可见本地 `.env` 覆盖 TCP fallback 端口；只适合本机联调。`.env.example` 中 TCP fallback 发布 host 为 `SERVICE_TCP_FALLBACK_ADVERTISED_HOST=127.0.0.1`，端口代码默认 `PROXY_PORT + 10000`。 |
| `apps/game-proxy/.env.example`、`apps/game-proxy/.env` | `PROXY_ADMIN_HOST` / `PROXY_ADMIN_PORT`、`SERVICE_ADMIN_BIND_HOST`、`SERVICE_ADMIN_ADVERTISED_HOST` | `127.0.0.1:7101` | `game-proxy.admin` HTTP 控制面默认；生产必须放在内网边界，消费者严格模式通过 registry 发现。 |
| `apps/game-proxy/.env.example`、`apps/game-proxy/.env` | `PROXY_LOCAL_SOCKET_NAME` | `myserver-game-proxy.sock` | `game-proxy` 自身本地 socket 名，不是跨服务发现目标。 |
| `apps/chat-server/.env.example` | `CHAT_BIND_ADDR`、`SERVICE_BIND_HOST`、`SERVICE_PUBLIC_HOST`、`CHAT_PUBLIC_HOST` | `0.0.0.0:9001`、发布示例 `127.0.0.1` | `chat-server.tcp` 自身监听；`0.0.0.0` 只用于 bind，发布时会规避通配地址。 |
| `apps/match-service/.env.example` | `MATCH_BIND_ADDR`、`SERVICE_BIND_HOST`、`SERVICE_PUBLIC_HOST`、`MATCH_PUBLIC_HOST` | `0.0.0.0:9002`、发布示例 `127.0.0.1` | `match-service.grpc` 自身监听；消费者严格模式发现 `match-service.grpc`，不使用静态地址。 |
| `apps/mail-service/.env.example`、`apps/mail-service/.env` | `HOST` / `MAIL_PORT`、`SERVICE_BIND_HOST`、`SERVICE_PUBLIC_HOST` | `127.0.0.1:9003` | `mail-service.http` 自身监听；auth-http 可选暴露该 endpoint 时从 registry 发现。 |
| `apps/announce-service/.env.example`、`apps/announce-service/.env` | `HOST` / `ANNOUNCE_PORT`、`SERVICE_BIND_HOST`、`SERVICE_PUBLIC_HOST` | `127.0.0.1:9004` | `announce-service.http` 自身监听；auth-http 可选暴露该 endpoint 时从 registry 发现。 |
| `apps/chat-server/.env` | `GAME_HOST` / `GAME_PORT` | `127.0.0.1:7001` | 可见本地 `.env` 中的旧值；当前 `apps/chat-server/src` 未检索到读取这两个变量，不能作为有效消费者配置依据。 |

### 2. 基础设施入口

| 文件 | 变量 / 配置 | 默认值或示例值 | 当前口径 / 风险 |
|---|---|---|---|
| 各服务 `.env.example`、可见 `.env` | `REDIS_URL`、`REDIS_KEY_PREFIX` | 本地默认多为 `redis://127.0.0.1:6379`，prefix 为空 | Redis 同时承载 session、ticket、限流、registry、route store 和 metrics snapshots；这是基础设施入口，不是业务服务直连。 |
| 各服务 `.env.test.example` | `REGISTRY_ENABLED=true`、`DISCOVERY_REQUIRED=true`、`REGISTRY_URL=redis://127.0.0.1:6379` | 测试示例使用本机 Redis | 测试严格发现只保留 registry 入口，没有发现 game/admin 直连变量。 |
| 各服务 `.env.production.example` | `REGISTRY_ENABLED=true`、`DISCOVERY_REQUIRED=true`、`REGISTRY_URL=redis://redis:6379` | 生产示例使用容器 / DNS 名 `redis` | 生产示例只保留 registry Redis 入口；未发现生产示例里保留 `GAME_PROXY_HOST`、`GAME_SERVER_ADMIN_HOST`、`MATCH_SERVICE_ADDR` 或 `UPSTREAM_LOCAL_SOCKET_NAME` 等消费者直连变量。 |
| `apps/game-proxy/.env.example` | `PROXY_ROUTE_STORE_REDIS_URL`、`PROXY_ROUTE_STORE_KEY_PREFIX` | `redis://127.0.0.1:6379`、空 | route store 可独立配置 Redis；未设置时代码会复用 registry / Redis 配置。 |
| `apps/auth-http/.env.example`、`apps/admin-api/.env.example`、`apps/chat-server/.env.example`、`apps/game-server/.env.example`、`apps/game-proxy/.env.example`、`apps/match-service/.env.example`、`apps/mail-service/.env.example`、`apps/announce-service/.env.example`、`apps/metrics-collector/.env.example` | `NATS_URL` | `nats://127.0.0.1:4222` | Core NATS 基础设施入口，用于 metrics、邮件通知、session kick、GM 广播等事件；不是 service registry。 |
| Node / Rust 服务 `.env.example`、可见 `.env` | `DATABASE_URL` | 示例多为 `postgres://postgres:password@127.0.0.1:5432/...`；可见本地 `.env` 使用本机开发库地址 | PostgreSQL 基础设施入口；可见 `.env` 中的本机密码只表示开发机配置，不应复制到测试/生产。 |
| `scripts/dev-metrics-collector.ps1` | `NATS_URL`、`REDIS_URL` | 缺省时写入 `nats://127.0.0.1:4222`、`redis://127.0.0.1:6379` | 本地启动 metrics collector 的基础设施默认值。 |
| `scripts/rollout-three-process-drill.ps1` | `RegistryUrl` / `RedisUrl` / `RegistryKeyPrefix` | 默认 `redis://127.0.0.1:6379`、prefix 为空 | 灰度演练脚本读取 registry snapshot 的基础设施入口；严格模式下业务 endpoint 必须从 registry 解析。 |

### 3. 已标注 Local fallback only 的消费者直连变量

| 文件 | 变量 / 配置 | 默认值或示例值 | 当前口径 / 风险 |
|---|---|---|---|
| `apps/auth-http/.env.example` | `GAME_PROXY_HOST` / `GAME_PROXY_PORT` | `127.0.0.1:4000` | `Local fallback only`：仅在 registry discovery disabled 且 discovery 非严格时回填登录响应兼容字段；strict/test/production 忽略，目标应为 `game-proxy.client`。 |
| `apps/auth-http/.env.example` | `GAME_SERVER_ADMIN_HOST` / `GAME_SERVER_ADMIN_PORT` | `127.0.0.1:7500` | `Local fallback only`：仅本地非严格访问 `game-server.admin`；测试/生产通过 registry 发现，且可由 `DISALLOW_LEGACY_DIRECT_CONFIG=true` 禁止遗留直连变量。 |
| `apps/admin-api/.env.example` | `GAME_SERVER_ADMIN_HOST` / `GAME_SERVER_ADMIN_PORT` | `127.0.0.1:7500` | `Local fallback only`：GM / 玩家控制面本地 fallback；多实例写操作仍要求 `targetInstanceId`，严格环境不应直连。 |
| `apps/admin-api/.env.example` | `GAME_PROXY_ADMIN_HOST` / `GAME_PROXY_ADMIN_PORT` | 注释示例 `127.0.0.1:7101` | `Local fallback only` 且默认注释；监控 / rollout 控制面严格环境发现 `game-proxy.admin`。 |
| `apps/mail-service/.env.example` | `GAME_SERVER_ADMIN_HOST` / `GAME_SERVER_ADMIN_PORT` | 注释示例 `127.0.0.1:7500` | `Local fallback only` 且默认注释；邮件附件发放严格环境发现 `game-server.admin`，多实例要求显式目标。 |
| `apps/game-server/.env.example` | `MATCH_SERVICE_ADDR` | `http://127.0.0.1:9002` | `Local fallback only`：仅 registry disabled 且非严格时直连 `match-service.grpc`；strict/test/production 要发现 `match-service.grpc`。 |
| `apps/game-server/.env.example` | `GAME_INTERNAL_SOCKET_NAME` | `myserver-game-server-internal.sock` | 自身 internal socket 默认名；作为发布 endpoint 的本地默认，不是外部消费者测试/生产直连建议。 |
| `apps/game-proxy/.env.example` | `UPSTREAM_SERVER_ID`、`UPSTREAM_LOCAL_SOCKET_NAME` | 注释示例 `game-server-1`、`myserver-game-server.sock` | `Local fallback only` 且默认注释；strict/test/production 必须发现 `game-server.proxy-local`，不回退到 `internal`。 |
| `apps/match-service/.env.example` | `GAME_SERVER_INTERNAL_SOCKET_NAME`、`GAME_INTERNAL_SOCKET_NAME` | 注释示例 `myserver-game-server-internal.sock` | `Local fallback only` / legacy alias，默认注释；strict/test/production 发现 `game-server.internal`。 |

### 4. scripts 和 mock-client 固定端口 / URL

| 文件 | 配置 | 默认值或示例值 | 当前口径 / 风险 |
|---|---|---|---|
| `scripts/dev-announce.ps1` | `ListenHost` / `Port`，写入 `HOST` / `ANNOUNCE_PORT` | `127.0.0.1`、`9004` | 本地启动公告服务的 bind 默认值。 |
| `scripts/dev-game.ps1` | `GAME_PORT`、`ADMIN_PORT`、`GAME_LOCAL_SOCKET_NAME` | 默认按参数写 `GAME_PORT`；单实例 admin 默认 `7500`，多实例常用 `Port + 1`；socket 为 `myserver-<InstanceId>.sock` | 本地启动 game-server 的端口 / socket 生成逻辑。 |
| `scripts/dev-match.ps1` | `MATCH_BIND_ADDR` | `0.0.0.0:<Port>`，默认端口由脚本参数决定 | 本地启动 match-service 的 bind 默认值。 |
| `scripts/dev-metrics-collector.ps1` | `NATS_URL` / `REDIS_URL` | `nats://127.0.0.1:4222`、`redis://127.0.0.1:6379` | 本地 worker 依赖入口。 |
| `scripts/dev-stack.ps1` | 本地端口输出和探测 | 输出 auth/admin/proxy/game/chat/match/mail/announce 等本地端口 | 已标注输出是 local bind/probe defaults，不是 registry discovery output。 |
| `scripts/rollout-three-process-drill.ps1` | `MYSERVER_OLD_GAME_PORT`、`MYSERVER_NEW_GAME_PORT` | `7000`、`7001` | 手工启动提示；脚本注释说明 player/control endpoint 通过 registry discovery 解析。 |
| `scripts/rollout-three-process-drill.ps1` | `MYSERVER_OLD_GAME_ADMIN_HOST` / `PORT`、`MYSERVER_NEW_GAME_ADMIN_HOST` / `PORT` | `127.0.0.1:7500`、`127.0.0.1:7501` | `local/manual fallback only`；strict/test/production 必须使用 registry 发现 old/new `game-server.admin`。 |
| `scripts/rollout-three-process-drill.ps1` | `MYSERVER_AUTH_BASE_URL`、`MYSERVER_PROXY_ADMIN_URL` | `http://127.0.0.1:3000`、`http://127.0.0.1:7101` | `local/manual fallback only`；严格环境 discovery required 时发现 `auth-http.internal` 和 `game-proxy.admin`。 |
| `tools/mock-client/src/args.js`、`tools/mock-client/help.txt`、`tools/mock-client/README.md` | `--http-base-url`、`--host`、`--port` | `http://127.0.0.1:3000`、`127.0.0.1`、TCP fallback 默认 `14000` | mock-client 本地联调默认；登录成功后仍可使用 auth 返回服务信息，不代表测试/线上固定入口。 |
| `tools/mock-client/src/args.js`、`tools/mock-client/help.txt`、`tools/mock-client/README.md` | `--chat-port`、`--mail-base-url`、`--announce-base-url` | 示例 `9001`、`http://127.0.0.1:9003`、`http://127.0.0.1:9004` | 周边服务本地场景参数；主要用于 mock-client 手工联调和帮助文本。 |
| `tools/mock-client/src/rollout-transfer-cli.js`、`tools/mock-client/src/rollout-fault-drill-cli.js`、`tools/mock-client/src/rollout-transfer.js`、`tools/mock-client/src/rollout-fault-drill.js` | old/new admin host/port、proxy admin URL | `127.0.0.1:7500`、`127.0.0.1:7501`、`http://127.0.0.1:7101` | CLI 帮助已标注仅 manual local drills；测试/预发/生产应优先走 `scripts/rollout-three-process-drill.ps1` 的 registry discovery 解析结果。 |
| `tools/check-registry-schema.js` | schema fixture endpoint | `127.0.0.1`、`9004`、`C:/tmp/game-server.sock`、`proxy.sock` 等 | registry schema 单元检查样例数据，不是运行配置。 |
| `tools/test-security-and-ticket.js`、`scripts/test-auth-http-login.ps1` | auth base URL | `http://127.0.0.1:3000` | 本地 auth-http 接口探测 / 安全检查默认值。 |

### 5. docs 中仍保留的固定端口说明

| 文件 | 固定值 | 当前口径 / 风险 |
|---|---|---|
| `AGENTS.md`、`apps/port.txt`、`docs/总览/整体架构.md` | `auth-http=3000`、`admin-api=3001`、`admin-web=3002`、`game-proxy=4000`、`game-server=7000`、`game-server admin=7500`、`game-proxy admin=7101`、`chat=9001`、`match=9002`、`mail=9003`、`announce=9004` | 默认端口表，用于本地开发和架构说明；不应作为测试/线上消费者直连建议。 |
| `docs/协议与客户端/外部客户端接入说明.md` | `http://127.0.0.1:3000`、`game-proxy:4000`、TCP fallback `14000`、直连调试 `game-server:7000` | 明确是本地入口或直连调试；正式客户端入口是登录响应给出的 `game-proxy`。 |
| `docs/游戏服与接入层/Rust游戏服开发指南.md` | `GAME_HOST:GAME_PORT=127.0.0.1:7000`、`ADMIN_HOST:ADMIN_PORT=127.0.0.1:7500` | 开发指南中的自身监听默认值。 |
| `docs/游戏服与接入层/game-proxy热切换代理设计.md` | `PROXY_PORT=4000`、`PROXY_TCP_FALLBACK_PORT=PROXY_PORT+10000`、`PROXY_ADMIN_PORT=7101`、`UPSTREAM_SERVER_ID`、`UPSTREAM_LOCAL_SOCKET_NAME`、`REGISTRY_URL=redis://127.0.0.1:6379`、`PROXY_ROLLOUT_DRAIN_STATUS_URL=http://127.0.0.1:3000/...` | 配置表保留本地默认和兼容字段；其中 upstream 静态配置只应视为本地 fallback，严格环境依赖 registry。 |
| `docs/周边服务/服务注册中心设计.md` | `UPSTREAM_LOCAL_SOCKET_NAME=myserver-game-server-001.sock`、`GAME_SERVER_INTERNAL_SOCKET_NAME` | 文档已标注本地开发兼容或后续移除测试/线上依赖；不作为线上直连建议。 |
| `docs/周边服务/匹配服务设计.md`、`docs/总览/整体架构.md` | `GAME_SERVER_INTERNAL_SOCKET_NAME` | 说明 match-service 本地 fallback；测试/线上应修复 registry 数据。 |
| `docs/后台与运维/管理后台设计.md`、`docs/安全与监控/监控设计.md` | `GAME_SERVER_ADMIN_HOST` / `GAME_SERVER_ADMIN_PORT`、`GAME_PROXY_ADMIN_HOST` / `GAME_PROXY_ADMIN_PORT` | 管理后台文档已补充本地 fallback / registry 发现口径；监控设计中仍有旧变量描述，迁移后续可继续把表述收敛为 registry-first。 |
| `docs/后台与运维/三进程灰度演练手册.md`、`docs/rollout-three-process-drill-runbook.md`、`docs/rollout-fault-drill-runbook.md` | old/new admin `7500/7501`、proxy admin `7101`、auth `3000` | 演练文档中的 manual local drill 参数；严格环境应使用 registry discovery 解析 endpoint。 |
| `docs/安全与监控/限流与安全现状.md` | 多处 `.env.example` 摘录中的 `PROXY_HOST`、`PROXY_PORT`、`PROXY_ADMIN_PORT`、`CHAT_BIND_ADDR`、`REDIS_URL`、`NATS_URL`、`DATABASE_URL` 等 | 安全现状中的示例配置；多处已说明监听地址 / admin 口 / chat bind 不是测试/线上直连建议。 |

### 6. 扫描结论

- `.env.test.example` 和 `.env.production.example` 当前只保留 `REGISTRY_ENABLED=true`、`DISCOVERY_REQUIRED=true` 和 `REGISTRY_URL`，未发现测试/生产示例中残留 `GAME_PROXY_HOST`、`MATCH_SERVICE_ADDR`、`GAME_SERVER_ADMIN_HOST`、`GAME_SERVER_ADMIN_PORT`、`UPSTREAM_LOCAL_SOCKET_NAME`、`GAME_SERVER_INTERNAL_SOCKET_NAME` 这类消费者直连配置。
- `.env.example` 中仍保留的消费者直连变量已经标注为 `Local fallback only` 或默认注释，当前口径是本地 registry disabled 且 discovery 非严格时才可用；严格环境会忽略、告警或通过 `DISALLOW_LEGACY_DIRECT_CONFIG=true` 拒绝。
- 可见 `.env` 主要是本机开发覆盖值；其中 `apps/chat-server/.env` 的 `GAME_HOST` / `GAME_PORT` 未在当前 `apps/chat-server/src` 中发现读取路径，属于旧本地残留风险，需要后续清理或确认是否仍有外部脚本依赖。
- `scripts` 和 `tools/mock-client` 保留了大量 `127.0.0.1` 默认值，但主要用于本地栈绑定、端口探测、registry fixture / dry-run、帮助文本和 manual drill；灰度主脚本已明确 strict/test/production 必须通过 registry discovery 解析 endpoint。
- 按本次检索口径，未发现未标注的测试/线上消费者直连残留；仍需注意文档中的默认端口表和历史设计说明可能被误读，后续文档收敛应继续统一为“默认监听值 / 本地 fallback / registry-first”三类表述。
