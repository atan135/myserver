# 服务发现迁移基线

本文是统一服务发现迁移的 M0 基线，只盘点当前代码中的注册事实。后续还需要继续补充：消费发现状态、固定配置扫描、统一入口/准入/本地 fallback 口径。

端口和地址只记录本地默认监听值或默认发布值，用于理解现状；不要把这些默认值当作测试或线上推荐直连配置。

## 当前注册状态

当前 Redis service registry 的通用数据模型来自 `packages/service-registry`：

- Rust 服务使用 `packages/service-registry/src/client.rs` 的 `RegistryClient`，实例写入 `service:<service>:instances:<instance>` 的 `data` 字段，心跳写入 `heartbeat:<service>:<instance>`，默认心跳 TTL 为 30 秒。
- Node 服务各自有本地 `src/registry-client.js`，但复用 `packages/service-registry/node/registry-schema.js` 生成 payload 和 key。它们同样写 `data` 字段和 `heartbeat:<service>:<instance>`。
- Node 周边服务配置里有 `REGISTRY_ENABLED`/`DISCOVERY_REQUIRED` 用于发现侧校验，但自身注册启动代码当前直接调用 `registryClient.register()`；注册失败只记录 `startup.registry_failed`，不会因为默认 `REGISTRY_ENABLED=false` 自动跳过注册。

| 服务 | 当前是否注册 | 注册实现位置 | service name / instance id 来源 | 复用方式 |
|---|---|---|---|---|
| `game-server` | `REGISTRY_ENABLED=true` 时注册 | `apps/game-server/src/main.rs`，配置在 `apps/game-server/src/config.rs` | `SERVICE_NAME` 默认 `game-server`；`SERVICE_INSTANCE_ID` 默认 `<service_name>-<GAME_PORT>`，本地默认 `game-server-7000` | Rust `packages/service-registry` |
| `game-proxy` | `REGISTRY_ENABLED=true` 时注册 | `apps/game-proxy/src/main.rs`，配置在 `apps/game-proxy/src/config.rs` | `SERVICE_NAME` 默认 `game-proxy`；`SERVICE_INSTANCE_ID` 默认 `<service_name>-<PROXY_PORT>`，本地默认 `game-proxy-4000` | Rust `packages/service-registry` |
| `auth-http` | 启动时尝试注册 | `apps/auth-http/src/main.ts`、`apps/auth-http/src/registry-client.js`、注入在 `apps/auth-http/src/app.module.ts` | `SERVICE_NAME` 默认 `auth-http`；`SERVICE_INSTANCE_ID` 默认 `auth-http-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js` |
| `chat-server` | `REGISTRY_ENABLED=true` 时注册 | `apps/chat-server/src/main.rs` | `SERVICE_NAME` 默认 `chat-server`；`SERVICE_INSTANCE_ID` 默认 `chat-server-001` | Rust `packages/service-registry` |
| `match-service` | `REGISTRY_ENABLED=true` 时注册 | `apps/match-service/src/main.rs`，配置在 `apps/match-service/src/config.rs` | `SERVICE_NAME` 默认 `match-service`；`SERVICE_INSTANCE_ID` 默认 `match-service-<MATCH_BIND_ADDR port>`，本地默认 `match-service-9002` | Rust `packages/service-registry` |
| `mail-service` | 启动时尝试注册 | `apps/mail-service/src/main.ts`、`apps/mail-service/src/registry-client.js` | `SERVICE_NAME` 默认 `mail-service`；`SERVICE_INSTANCE_ID` 默认 `mail-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js` |
| `announce-service` | 启动时尝试注册 | `apps/announce-service/src/main.ts`、`apps/announce-service/src/registry-client.js` | `SERVICE_NAME` 默认 `announce-service`；`SERVICE_INSTANCE_ID` 默认 `announce-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js` |
| `admin-api` | 启动时尝试注册 | `apps/admin-api/src/main.ts`、`apps/admin-api/src/registry-client.js` | `SERVICE_NAME` 默认 `admin-api`；`SERVICE_INSTANCE_ID` 默认 `admin-api-001` | 独立 Node client，复用 `packages/service-registry/node/registry-schema.js`，并复用 discovery helper 刷新 game-server/game-proxy admin endpoint |
| `metrics-collector` | 不注册 | `apps/metrics-collector/src/server.js`、`apps/metrics-collector/src/registry-client.js`、配置在 `apps/metrics-collector/src/config.js` | `SERVICE_NAME` 默认 `metrics-collector`；`SERVICE_INSTANCE_ID` 默认 `metrics-collector-001`，仅用于状态输出 | 显式禁止注册；没有真实可连接 endpoint |

### 发布 endpoints

| 服务 | 当前发布 endpoints | 本地默认监听/发布值参考 |
|---|---|---|
| `game-server` | `client` / `tcp` / `internal`；`admin` / `tcp` / `admin`；`internal` / `local_socket` / `local`；`proxy-local` / `local_socket` / `local` | `GAME_PORT=7000`；`ADMIN_PORT=7500`；`GAME_INTERNAL_SOCKET_NAME` 默认 `myserver-game-server-internal.sock`；`GAME_LOCAL_SOCKET_NAME` 默认 `myserver-game-server.sock` |
| `game-proxy` | `client` / `kcp` / `public`；`client-tcp-fallback` / `tcp` / `public`；`admin` / `http` / `admin` | `PROXY_PORT=4000`；`PROXY_TCP_FALLBACK_PORT` 默认 `PROXY_PORT + 10000`，即 `14000`；`PROXY_ADMIN_PORT=7101` |
| `auth-http` | `http` / `http` / `public`；`internal` / `http` / `internal` | `PORT=3000` |
| `chat-server` | `tcp` / `tcp` / `internal` | `CHAT_BIND_ADDR=0.0.0.0:9001`，发布 host 会规避 `0.0.0.0`/`::` 并回退到 `127.0.0.1` |
| `match-service` | `grpc` / `grpc` / `internal` | `MATCH_BIND_ADDR=0.0.0.0:9002`，发布 host 会规避 `0.0.0.0`/`::` 并回退到 `127.0.0.1` |
| `mail-service` | `http` / `http` / `internal` | `MAIL_PORT=9003` |
| `announce-service` | `http` / `http` / `internal` | `ANNOUNCE_PORT=9004` |
| `admin-api` | `http` / `http` / `admin` | `PORT=3001` |
| `metrics-collector` | 无 | 无监听 endpoint；消费 NATS metrics 并写 Redis snapshots |

各服务发布 host 都有“不要发布通配监听地址”的处理：Rust 服务在 `build_service_instance` 附近通过 `published_host`/`advertised_host_from_env` 归一化，Node 服务在各自 `registry-client.js` 的 `publishedHostFromConfig` 中归一化。

### Heartbeat 与 deregister

| 服务 | heartbeat 行为 | deregister 行为 |
|---|---|---|
| `game-server` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，间隔来自 `REGISTRY_HEARTBEAT_INTERVAL`，默认 10 秒，TTL 30 秒 | `server::run` 返回后 abort heartbeat task，再 `deregister()` 删除实例和 heartbeat key |
| `game-proxy` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，当前未调用 `with_heartbeat_interval`，使用 registry 包默认 10 秒，TTL 30 秒 | `proxy_server::run` 返回后 abort heartbeat task，再 `deregister()` 删除实例和 heartbeat key |
| `chat-server` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，间隔来自 `REGISTRY_HEARTBEAT_INTERVAL`，默认 10 秒，TTL 30 秒 | 主循环返回后释放相关任务，再 `deregister()` 删除实例和 heartbeat key |
| `match-service` | `register()` 时写一次 heartbeat；随后 `start_heartbeat_task()`，间隔来自 `REGISTRY_HEARTBEAT_INTERVAL`，默认 10 秒，TTL 30 秒 | `server::run` 返回后 abort heartbeat task，再 `deregister()` 删除实例和 heartbeat key |
| `auth-http` | `main.ts` 调用 `startHeartbeat(10)`；Node client 立即 `setex` 一次，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()`，再 `deregister()` 删除实例和 heartbeat key |
| `mail-service` | `main.ts` 调用 `startHeartbeat(10)`；立即写 heartbeat，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()`，再 `deregister()` 删除实例和 heartbeat key |
| `announce-service` | `main.ts` 调用 `startHeartbeat(10)`；立即写 heartbeat，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()`，再 `deregister()` 删除实例和 heartbeat key |
| `admin-api` | `main.ts` 调用 `startHeartbeat(10)`；立即写 heartbeat，之后每 10 秒续写，TTL 30 秒 | `closeNestApp` 先 `stopHeartbeat()` 和 `stopDiscoveryRefresh()`，再 `deregister()` 删除实例和 heartbeat key |
| `metrics-collector` | 无 registry heartbeat；业务层只写 `metrics:heartbeat:<service>` 和 `metrics:heartbeat:<service>:<instance>` 表示 metrics 数据新鲜度 | 无 registry deregister |

### metrics-collector 不注册的代码依据

`apps/metrics-collector/src/registry-client.js` 的 `maybeRegisterService()` 默认返回 `registered: false`，原因文本是：它消费 NATS metrics 并写 Redis snapshots，但没有可连接服务 endpoint。若设置 `SERVICE_REGISTRY_REGISTER=true`，该函数会直接抛错，说明在暴露真实 `http`/`tcp`/`local_socket` endpoint 之前不支持注册。

因此 `metrics-collector` 当前不是服务发现生产者。它写入的 `metrics:*` 和 `metrics:heartbeat:*` key 是指标快照与指标新鲜度，不是 service registry 实例记录。
