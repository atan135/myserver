# 服务器初始化实操

## 1. 定位

本文记录 MyServer 单机 Docker 宿主机的已验证初始化流程。它是[服务器 Docker 初始化与更新](./服务器Docker初始化与更新.md)的执行补充，不替代首次发布、数据库迁移、服务发现校验、接流量或回滚流程。

本文不记录主机名、公网 IP、SSH 私钥、镜像仓库凭据、镜像加速器账号标识或生产 secret。此类信息只能留在受控密码管理系统或服务器本地 `secrets/` 目录。

## 2. 当前初始化结果

已验证的宿主机基线：

| 项目 | 当前状态 |
|---|---|
| 操作系统 | Ubuntu 24.04 LTS，`x86_64` |
| 计算资源 | 4 vCPU，约 7.7 GiB 内存，约 3.8 GiB swap |
| 内核能力 | cgroup v2、NTP 时间同步正常 |
| 根分区 | 约 39 GB，仅承载系统、系统包与管理员工具，不承载 Docker 或 containerd 数据 |
| 数据盘 | ext4 挂载于 `/data`，容量约 59 GB |
| Docker 数据目录 | `/data/myserver/docker` |
| containerd 数据目录 | `/data/myserver/containerd` |
| Docker 日志 | `local` driver，单容器最多 `20m x 3` |
| Redis 内核参数 | `vm.overcommit_memory=1` |
| Redis 持久化 | 发布清单使用 AOF `everysec`，显式禁用 RDB snapshot（`save ""`） |
| Docker Hub 代理 | `https://docker.m.daocloud.io`，已通过 `hello-world` 验证 |

当前 `60 GB` 数据盘满足单机正式发布的至少 `50 GB` 基线。该容量仍较紧凑，必须持续监控 PostgreSQL、Redis AOF、日志、Docker 与 containerd 镜像占用；可恢复备份应使用独立磁盘、对象存储或异机存储，不与容器运行时数据争抢空间。

当前仅完成 Docker 宿主机初始化，尚未部署 MyServer Compose 清单、业务镜像、数据库 migration、TLS 反向代理或玩家入口。

Vector 普通运行日志采集当前尚未部署。Vector 完成后，目标输出根目录为 `/data/myserver/log`，按服务、实例和 UTC 日期保存；在此之前请通过 `docker logs` 查看 Docker 保留窗口内的日志。完整架构和目标态边界见[服务端日志采集与留存设计](../../安全与监控/服务端日志采集与留存设计.md)。

阶段 1 固定的生产采集 allowlist 为：`game-server`、`game-proxy`、`auth-http`、`admin-api`、`chat-server`、`match-service`、`mail-service`、`announce-service`、`metrics-collector`。其中 `metrics-collector` 仍是 NATS/Redis/PostgreSQL metrics 的消费者；它自身的 console 启动、连接和错误日志由 Vector 采集，但不改造成 metrics 专用日志框架。

查询入口必须按职责分开：普通运行日志查询 `/data/myserver/log/<service>/<YYYY-MM-DD>/` 的已关闭 JSONL 分片，Vector 故障时使用 `docker logs` 的 `local` driver 窗口；admin/security audit 查询 PostgreSQL `admin_audit_logs`、`security_audit_logs`（及连接审计表）或 admin-api 审计入口；数据库迁移/部署审计查询各库 `_myserver_migration_audit` 与 SQLx history；metrics 查询 `metrics-collector` 写入的 Redis/PostgreSQL metrics v2。各类数据不得相互替代。

v2 release bundle 携带固定 Vector `0.47.0` 的 `vector/vector.yaml` 和 `vector/vector.service`，安装目标为 `/usr/bin/vector`、`/etc/vector/vector.yaml`、`/var/lib/vector` 及 `/data/myserver/log`。首次初始化只校验和安装文件，不自动启动服务：

```bash
sudo /data/myserver/release/<release-id>/scripts/verify-vector.sh \
  --source /data/myserver/release/<release-id>/vector --offline
sudo /data/myserver/release/<release-id>/scripts/install-vector.sh \
  --source /data/myserver/release/<release-id>/vector
sudo vector validate --config /etc/vector/vector.yaml
```

安装后使用 `/data/myserver/release/<release-id>/scripts/vector-status.sh` 查看 unit、checkpoint、活动/已关闭分片和磁盘状态；是否启用或启动 systemd 必须在完成阶段 4 的发布准入后单独执行。

单分片达到 `256 MiB` 后由受控 `/data/myserver/release/<release-id>/scripts/rotate-vector-files.sh` 轮转：默认只报告候选，获批后加 `--apply`。脚本会短暂停止 Vector 使 file sink flush/close，执行 `sync -f` 和原子 rename 为递增 shard，再启动 Vector；任何 `.jsonl.open` 活动文件不得被归档工具读取。

清理脚本 `/data/myserver/release/<release-id>/vector/prune-vector-files.mjs` 默认只读；它仅对 `archive-manifest.jsonl` 已确认哈希/大小的过期关闭分片给出候选。获批后必须使用 `--apply --confirm vector-retention-v2`，删除前写入 `retention-actions.jsonl`；未归档分片和活动 `.open` 文件永不删除。

## 3. 初始化前检查

先确认数据盘已经挂载且不是待格式化的空白设备。当前实例直接复用已有 `/data`，不得执行 `mkfs`、修改其 UUID 或覆盖已有 `/etc/fstab` 条目。

```bash
findmnt /data
lsblk -f
df -hT / /data
stat -fc %T /sys/fs/cgroup
timedatectl status
```

若 `/data` 未挂载、文件系统类型不是预期的 ext4/xfs，或目录已有不明业务数据，应停止并先完成磁盘归属确认。

## 4. 安装 Docker Engine

使用 Docker 官方 Ubuntu 软件源，不使用 Snap 版 Docker，也不在生产服务器安装 Node.js、Rust 或项目构建工具。

```bash
sudo apt-get update
sudo apt-get install -y ca-certificates curl

sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc

echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" \
  | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt-get update
sudo apt-get install -y \
  docker-ce \
  docker-ce-cli \
  containerd.io \
  docker-buildx-plugin \
  docker-compose-plugin
```

安装后先停止 Docker 和 containerd，再写入数据目录与运行时配置，避免后续镜像、快照与 volume 默认堆积到根分区：

```bash
sudo systemctl stop docker.service docker.socket containerd.service
```

## 5. 数据目录与权限

```bash
sudo install -d -m 0755 \
  /data/myserver/docker \
  /data/myserver/containerd \
  /data/myserver/release \
  /data/myserver/data/postgres \
  /data/myserver/data/redis \
  /data/myserver/data/nats \
  /data/myserver/backups \
  /data/myserver/log \
  /data/myserver/sockets

sudo install -d -m 0700 /data/myserver/secrets
```

由 `gameops` 接收 release、生成本机 secret 并执行 Docker Compose 时，额外设置最小可用权限：

```bash
sudo chown root:gameops /data/myserver/release
sudo chmod 2770 /data/myserver/release
sudo chown gameops:gameops /data/myserver/secrets
sudo chmod 0700 /data/myserver/secrets
sudo usermod -aG docker gameops
```

`release/` 的 setgid 位保证每个 release 子目录继承 `gameops` 组；它不存放 secret。`secrets/` 只允许该受控运维账号读取，所有其中的 env 文件必须为 `0600`。加入 `docker` 组等价于较高宿主机权限，只应授予该受控账号。

不要在此阶段递归 `chown` PostgreSQL、Redis、NATS 数据目录。实际 Compose 镜像确定后，先确认其运行 UID/GID，再只对对应数据目录授予最小权限。

## 6. Docker daemon 配置

首次写入前必须确认 `/etc/docker/daemon.json` 不存在。若它已经存在，应合并合法 JSON 字段，不能用下列示例覆盖其中已有的代理、证书或其他受控设置。

```bash
sudo test ! -e /etc/docker/daemon.json \
  && echo "daemon.json 不存在，可创建"
```

当前单机配置如下：

```json
{
  "data-root": "/data/myserver/docker",
  "log-driver": "local",
  "log-opts": {
    "max-size": "20m",
    "max-file": "3"
  },
  "registry-mirrors": [
    "https://docker.m.daocloud.io"
  ]
}
```

其中：

- `data-root` 必须保留，不能回退到 `/var/lib/docker`。
- 使用 `local` 日志驱动，避免 `json-file` 以数百 MB 的单容器日志上限快速占满 60 GB 数据盘。该配置只提供有界短期缓冲；它不等同于按日期的长期日志留存，也不替代后续常驻 Vector。
- v2 Vector 目标参数为：单分片 `256 MiB`、默认保留最近 `14` 个 UTC 日期目录、磁盘队列上限 `1 GiB`、内存队列上限 `64 MiB`。`/data` 可用空间低于 `20%` 告警，低于 `10%` 进入保护模式，低于 `5%` 由运维扩容或转移数据；这些阈值不得通过静默删除未归档日志来规避。
- 镜像代理仅用于 Docker Hub 公共基础镜像拉取；它不是生产业务镜像仓库，也不应加入多个未经验证的公共地址。
- 不设置 `experimental`、`insecure-registries` 或无用途的 Builder 缓存配额。生产服务器不执行 `docker build`。

建议先写入临时文件并校验：

```bash
sudo tee /tmp/myserver-daemon.json > /dev/null <<'EOF'
{
  "data-root": "/data/myserver/docker",
  "log-driver": "local",
  "log-opts": {
    "max-size": "20m",
    "max-file": "3"
  },
  "registry-mirrors": [
    "https://docker.m.daocloud.io"
  ]
}
EOF

sudo dockerd --validate --config-file /tmp/myserver-daemon.json
sudo install -m 0644 /tmp/myserver-daemon.json /etc/docker/daemon.json
sudo rm /tmp/myserver-daemon.json
```

为确保 `/data` 挂载完成后才启动 Docker，添加 systemd drop-in：

```bash
sudo install -d -m 0755 /etc/systemd/system/docker.service.d

sudo tee /etc/systemd/system/docker.service.d/10-data-mount.conf > /dev/null <<'EOF'
[Unit]
RequiresMountsFor=/data/myserver/docker
EOF
```

## 7. containerd 配置

独立的 `containerd.service` 默认使用 `/var/lib/containerd`。Docker 的 `data-root` 不会覆盖该目录，因此必须在保留现有合法字段的前提下设置 containerd `root`：

```toml
# /etc/containerd/config.toml
disabled_plugins = ["cri"]
root = "/data/myserver/containerd"
```

首次初始化时可创建该文件；已有文件时只能合并 `root` 字段，不能覆盖已审阅的 registry、proxy 或运行时配置。为保证数据盘已挂载，添加与 Docker 相同的 systemd 依赖：

```bash
sudo install -d -m 0755 /etc/systemd/system/containerd.service.d

sudo tee /etc/systemd/system/containerd.service.d/10-data-mount.conf > /dev/null <<'EOF'
[Unit]
RequiresMountsFor=/data/myserver/containerd
EOF
```

已运行主机迁移 containerd root 必须在维护窗口完成：停止 Docker 与 containerd，使用保留权限、硬链接、ACL 和扩展属性的工具完整复制旧目录，修改配置后先启动 containerd、再启动 Docker 并核验全部容器健康。验证通过前保留旧目录作为回滚副本；不得直接删除或移动运行中的 `/var/lib/containerd`。

## 8. Redis 内核参数与启动

```bash
echo 'vm.overcommit_memory=1' \
  | sudo tee /etc/sysctl.d/99-myserver-redis.conf > /dev/null
sudo sysctl -p /etc/sysctl.d/99-myserver-redis.conf

sudo systemctl daemon-reload
sudo systemctl enable --now docker
```

## 9. 镜像代理验证

先验证代理本身可达：

```bash
curl -4 -sS -o /dev/null -w 'HTTP=%{http_code}\n' \
  --connect-timeout 10 \
  --max-time 20 \
  https://docker.m.daocloud.io/v2/
```

`200`、`401` 或 `404` 表示 Registry HTTP 端点可达。随后分别验证显式代理地址和普通 Docker Hub 镜像名：

```bash
sudo docker pull docker.m.daocloud.io/library/hello-world:latest
sudo docker pull hello-world:latest
sudo docker run --rm hello-world:latest
```

`Hello from Docker!` 表示 Docker client、daemon、镜像代理、镜像存储和容器运行链路均正常。

## 10. 完成后核验

```bash
sudo docker version
sudo docker compose version
sudo docker buildx version
sudo docker info --format \
  'cpus={{.NCPU}} memory={{.MemTotal}} root={{.DockerRootDir}} logging={{.LoggingDriver}} cgroup={{.CgroupVersion}}'

sysctl vm.overcommit_memory
df -hT /data /data/myserver/docker /data/myserver/containerd
systemctl is-active docker
systemctl is-active containerd
sudo ufw status verbose
```

Docker `root` 必须为 `/data/myserver/docker`，containerd `root` 必须为 `/data/myserver/containerd`，日志驱动必须为 `local`，cgroup 版本必须为 `2`，Docker 与 containerd 服务状态必须为 `active`。

Vector 尚未完成前，`/data/myserver/log` 仅作为预留目录；不要据此判断日志已经被持续采集。

## 11. 网络与上线前边界

- 防火墙和云安全组应继续默认拒绝入站。当前初始化阶段只保留受控 SSH 管理入口。
- 不开放 `3000`、`3001`、`7000`、`7500`、`7101`、`9001` 至 `9004`、`4222`、`8222`、`5432`、`6379` 等内部端口。
- 只有 Compose、TLS 反向代理、入口认证和服务发现就绪后，才按[服务器 Docker 初始化与更新](./服务器Docker初始化与更新.md)开放 `80/TCP`、`443/TCP`、`4000/UDP` 及实际需要的 TCP fallback。
- 管理后台必须走 VPN、零信任入口或 IP allowlist，不能直接暴露为无保护公网服务。

## 12. 后续必须完成的事项

1. 保持 `/data` 至少 50 GB，并为备份使用独立磁盘、对象存储或异机存储。
2. 建立受控私有镜像仓库。业务镜像和基础镜像应以不可变 digest 发布，生产服务器不执行 `docker build`、`npm install`、`cargo build` 或 `git pull`。
3. 实现并审阅 `deploy/docker/` Compose、Dockerfile、配置和 `images.lock.json`，详见[本地 Docker 镜像打包与发布](./本地Docker镜像打包与发布.md)。
4. 按[服务器 Docker 初始化与更新](./服务器Docker初始化与更新.md)完成数据库备份、migration runner、registry/readiness、drain、回滚和监控能力后，再接入外部流量。

## 13. Docker 配置回退

修改 `daemon.json` 前应先备份。镜像代理或新配置导致 Docker 无法正常启动时，使用备份回退：

```bash
sudo cp -a /etc/docker/daemon.json.bak.<timestamp> /etc/docker/daemon.json
sudo systemctl restart docker
sudo systemctl is-active docker
```

不得通过删除 `/data/myserver/docker`、执行 `docker system prune -a` 或重装 Docker 来处理配置问题；这些操作可能删除本地镜像、容器或 volume 数据。
